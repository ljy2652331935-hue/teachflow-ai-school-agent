# Open Questions

Last updated: 2026-07-02

## Product

- What is the first real pilot user flow: teacher-only, student-only, or teacher plus a small set of student aliases?
- How much student-facing AI support is acceptable for the first school trial?
- What export format matters most for teachers: Markdown, PDF, PPT, LMS import, or copyable text?
- What exact language should be used to avoid making learning-state labels feel judgmental?
- What school-admin views matter next after multi-class comparison: teacher workload, pilot feedback, or budget/readiness report?

## Data And Privacy

- What retention period should local pilot data use?
- Should student aliases be generated by TeachFlow or imported from a school system?
- What audit retention, export, and tamper-resistance level is required before a school trial?
- What data must be excluded from devlogs and demo fixtures beyond obvious secrets and real identities?
- Should browser workspace cache be cleared on every logout, every account switch, or both?
- What written pilot notice should teachers/schools use before any alias-only trial?
- Who is the school's designated safeguarding role for Level 3 learning wellbeing risk signals, and what exact escalation workflow should TeachFlow follow?
- What wording should the school approve for student-facing AI 学习关怀 consent, privacy, and non-therapy notices?

## Security

- What CSRF design should the local API use: double-submit token, session-bound token, or same-origin custom header?
- Which security headers should be enforced by the local server without breaking prototype pages?
- What rate limits and size limits should apply to student questions and stuck-signal notes?
- What production auth path is most realistic later: school SSO, password accounts, or district-managed identity?

## Architecture

- Should the next persistence layer be SQLite or continue with JSON until the workflow stabilizes?
- Should teacher/student prototype pages replace the older `app.js` workflow, or coexist as separate demo surfaces?
- Should `outputs/teachflow-v0.1` be renamed to match `v0.3`, or left alone to avoid churn? Pending confirmation.
- Which additional demo scenarios should be added next: humanities writing feedback, middle-school math, or school-wide teacher workload?

## AI/Agent Behavior

- When should deterministic local logic hand off to real LLM calls?
- What guardrails must be enforced before any student-facing generative answer?
- How should teacher edits feed back into future agent recommendations?
- Which student data, if any, may be sent to an external AI provider under a school-approved agreement?
- How should Student Check-in signals be retained, deleted, and excluded from ordinary teacher views when students keep them private?

## Testing

- Add browser-level tests for student question flow and teacher-visible question review.
- Add deeper browser tests for the school-admin class comparison channel and future teacher-workload views.
- Add regression tests for CSRF behavior once implemented.
