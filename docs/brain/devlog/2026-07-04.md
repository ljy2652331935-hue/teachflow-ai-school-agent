# Devlog

## Date

2026-07-04

## Goal

Prepare the active TeachFlow school-live MVP for an English demo by removing Chinese text from the runnable app, default state, tests, and demo-facing support files.

## Changes

- Localised `outputs/teachflow-school-live` to English across runtime pages, agent prompts, fallback copy, seed/default state, tests, and demo notes.
- Added and ran `scripts/finalize-english-demo-localization.mjs` for a broad localisation pass.
- Cleared the generated `scripts/cjk-strings.txt` report after localisation.
- Repaired mechanically affected JavaScript identifiers such as browser API casing and camelCase state fields.
- Performed a conservative cleanup to remove fallback text such as `learning detail`.

## Files

- `outputs/teachflow-school-live/**/*.js`
- `outputs/teachflow-school-live/**/*.html`
- `outputs/teachflow-school-live/**/*.md`
- `outputs/teachflow-school-live/**/*.json`
- `outputs/teachflow-school-live/scripts/finalize-english-demo-localization.mjs`
- `outputs/teachflow-school-live/scripts/cjk-strings.txt`

## Commands Run

- `node scripts/apply-en-gb-localization.mjs`
- `node scripts/finalize-english-demo-localization.mjs`
- `rg -n "\p{Han}" outputs\teachflow-school-live`
- `rg -n "learning detail" outputs\teachflow-school-live`
- `npm.cmd run check`

## Decisions

- Treated `outputs/teachflow-school-live` as the active demo app and prioritised it over the legacy `outputs/teachflow-v0.1` reference app.
- Kept internal project-brain documentation and the legacy v0.1 app outside the first localisation pass to avoid destabilising Codex guidance and old demo references.

## Issues

- A broad mechanical cleanup briefly affected JavaScript identifier casing, including browser APIs and camelCase fields. These were repaired and the full project check now passes.
- The wider repository still contains Chinese in internal docs and the legacy v0.1 reference app.

## Next

- For a fully English public repository, translate or archive the remaining internal docs and legacy v0.1 reference files.
- Run a browser-level smoke test before the pitch demo to visually confirm the teacher, pupil, and school admin pages.

## Follow-up Demo Copy Fix

## Goal

Remove visible Chinese and broken machine-translation fragments from the student/teacher Agent flow after manual demo review found strings such as old Chinese plan steps and `learning...` copy.

## Changes

- Rewrote the Student Agent briefing output so understanding status, next steps, stuck-signal drafts, and privacy notes are clean English.
- Cleaned the student portal's live channels for home, materials, assignment/question, teacher messages, AI support, wellbeing check-in, and progress.
- Added a workspace-state copy migration so old browser/localStorage strings are translated on read instead of reappearing after refresh.
- Rebuilt the Teacher Agent orchestrator with clean English while preserving the same API fields.
- Bumped school-live HTML asset versions to `20260704-copy-polish-2` so browsers load the new scripts.

## Files

- `outputs/teachflow-school-live/student-agent-orchestrator.js`
- `outputs/teachflow-school-live/outcome-evaluation-engine.js`
- `outputs/teachflow-school-live/student-prototype.js`
- `outputs/teachflow-school-live/workspace-state.js`
- `outputs/teachflow-school-live/teacher-agent-orchestrator.js`
- `outputs/teachflow-school-live/*-prototype.html`
- `outputs/teachflow-school-live/login.html`

## Commands Run

- `node --check student-prototype.js`
- `node --check teacher-agent-orchestrator.js`
- `node --check workspace-state.js`
- `npm.cmd run check`

## Decisions

- Used a runtime state migration instead of asking the developer to manually clear browser storage.
- Overrode the student portal's active demo channels with clean English templates to avoid relying on older machine-translated legacy templates.

## Issues

- Some non-active legacy templates, helper scripts, and tests still contain old machine-translation fragments. They are not currently shown by the cleaned student demo channels, but a full repository cleanup should remove them later.

## Next

- Run a visual browser smoke test of the student and teacher pages before recording or presenting the demo.
- Do a separate cleanup pass on `teacher-prototype.js` display copy, especially material studio and outcome panels, if the teacher page is part of the pitch walkthrough.

## UI Layout And Copy Polish

## Goal

Fix demo-facing English formatting issues where translated words were joined together and long labels overflowed cards, buttons, status pills, and panels.

## Changes

- Added a global copy-polish layer in `auth-ui.js` that cleans visible joined words such as `teacherpublish`, `learningsubmit`, `displayName`, and other old machine-translation fragments after page render.
- Added global anti-overflow CSS in `ethereal-ui.css` for teacher, student, school-admin, login, cards, buttons, status pills, records, and form fields.
- Updated page asset versions to `20260704-layout-polish` so browsers reload the fixed CSS and JS.
- Cleaned static role-header wording such as `AI School System`.
- Repaired the browser smoke-test helper so real-browser validation works again.

## Files

- `outputs/teachflow-school-live/auth-ui.js`
- `outputs/teachflow-school-live/ethereal-ui.css`
- `outputs/teachflow-school-live/student-prototype.html`
- `outputs/teachflow-school-live/teacher-prototype.html`
- `outputs/teachflow-school-live/login.html`
- `outputs/teachflow-school-live/school-admin-prototype.html`
- `outputs/teachflow-school-live/tests/browser-login-smoke.test.js`

## Commands Run

- `node --check auth-ui.js`
- `npm.cmd run check`
- `npm.cmd run test:browser-login`

## Decisions

- Used a global DOM-level copy polisher as a safety net because older templates and persisted demo data can still surface joined words.
- Used CSS wrapping and min-width safeguards rather than changing the overall information architecture.

## Issues

- Some source files still contain legacy machine-translated strings in inactive templates or fallback text. The new copy-polish layer protects the visible UI, but a deeper source cleanup remains useful.

## Next

- Do a visual pass on the teacher material studio and approval pages to replace remaining awkward source copy directly, not only through the runtime safety net.

## Residual English Demo Copy Fix

## Goal

Remove the remaining visible Chinese strings and broken joined English shown in the student support plan, teacher Agent action list, misconception/outcome metrics, and persisted teacher-action evidence.

## Changes

- Expanded the global copy-polish layer so visible Chinese text nodes from old browser state are detected and translated.
- Added targeted English replacements for the student next-step plan, teacher Agent actions, source chips, outcome feedback labels, and old teacher-action records.
- Rewrote the teacher Agent outcome panel, action cards, action history, and action-result copy directly in clean English.
- Cleaned the student AI support panel labels, memory list, stuck-signal options, and status text.
- Migrated `data/workspace-state.json` to remove persisted old fragments such as `teacheractionstuck point` and `learningneedssupport`.
- Bumped active page asset versions to `20260704-copy-fix`.

## Files

- `outputs/teachflow-school-live/auth-ui.js`
- `outputs/teachflow-school-live/workspace-state.js`
- `outputs/teachflow-school-live/teacher-prototype.js`
- `outputs/teachflow-school-live/student-prototype.js`
- `outputs/teachflow-school-live/teacher-prototype.html`
- `outputs/teachflow-school-live/student-prototype.html`
- `outputs/teachflow-school-live/login.html`
- `outputs/teachflow-school-live/data/workspace-state.json`

## Commands Run

- `node --check auth-ui.js`
- `node --check workspace-state.js`
- `node --check teacher-prototype.js`
- `node --check student-prototype.js`
- `npm.cmd run check`
- `npm.cmd run test:browser-login`

## Decisions

- Cleaned the persisted workspace data instead of relying only on DOM-level translation, because old stored strings were the main source of the screenshot issues.
- Kept the runtime copy polisher as a safety net for old localStorage or cached demo sessions.

## Issues

- Some replacement dictionaries intentionally keep old broken strings on the left-hand side so they can translate stale data at runtime.

## Next

- Refresh the teacher and student pages with the new `20260704-copy-fix` assets and visually check the exact panels from the screenshots.

## Personal Agent Copy Follow-up

## Goal

Remove the remaining mixed Chinese and joined English from the student Personal Agent support plan and teacher outcome detail records.

## Changes

- Added extra stale-copy translations for old student Agent summaries, teacher feedback steps, assignment feedback status, and outcome follow-up recommendations.
- Added student-page display helpers that rebuild the Personal Agent summary in English and prevent `undefined` from appearing in assignment status cards.
- Cleaned Personal Agent plan items, teacher-visible drafts, and support-card bodies before rendering.
- Cleaned persisted workspace data again so old strings such as `learningnowlearning`, `learningsymbolis`, `learning diagramdraft`, and `teacheraction` do not feed the UI.
- Updated active asset versions to `20260704-agent-copy-fix`.
- Fixed a remaining teacher detail label from `teacheraction` to `Teacher action`.

## Files

- `outputs/teachflow-school-live/auth-ui.js`
- `outputs/teachflow-school-live/workspace-state.js`
- `outputs/teachflow-school-live/student-prototype.js`
- `outputs/teachflow-school-live/teacher-prototype.js`
- `outputs/teachflow-school-live/student-prototype.html`
- `outputs/teachflow-school-live/teacher-prototype.html`
- `outputs/teachflow-school-live/login.html`
- `outputs/teachflow-school-live/data/workspace-state.json`

## Commands Run

- `node --check student-prototype.js`
- `node --check teacher-prototype.js`
- `node --check auth-ui.js`
- `node --check workspace-state.js`
- `npm.cmd run check`
- `npm.cmd run test:browser-login`

## Decisions

- Rebuilt the student support summary from clean fields at render time instead of trusting a cached Agent summary string.
- Kept stale-string translations in the global and workspace cleaners so old browser/cache data is still repaired on refresh.

## Issues

- Browser memory can still show old text until the page is refreshed and the new `20260704-agent-copy-fix` assets load.

## Next

- Use `Ctrl + F5` on both teacher and student pages before demo recording to force the new assets.

## School Admin Entry Restore

## Goal

Restore the school admin portal entry after the live workspace only showed teacher and pupil accounts.

## Changes

- Added a workspace normalization guard that ensures a `school-admin-demo` account exists whenever the current workspace has classes.
- Attached the school admin account to all current classes so `/api/session/accounts` can show it and the session store can log into it.
- Updated the login quick-account helper copy to mention school admin accounts.
- Re-normalized `outputs/teachflow-school-live/data/workspace-state.json` so the current local app immediately includes `school-admin-demo / Lead coordinator`.

## Files

- `outputs/teachflow-school-live/workspace-state.js`
- `outputs/teachflow-school-live/auth-ui.js`
- `outputs/teachflow-school-live/data/workspace-state.json`

## Commands Run

- `node --check workspace-state.js`
- `node --check auth-ui.js`
- `npm.cmd run check`
- `npm.cmd run test:browser-login`

## Decisions

- Treated school admin as a required portal identity for the MVP rather than optional demo data, so the entry is restored during state normalization.

## Issues

- If a workspace has no class yet, school admin login still cannot be created because sessions require a class boundary.

## Next

- Refresh `login.html`, choose `Lead coordinator`, and verify the school admin dashboard opens.

## Demo Content Seed

## Goal

Add enough realistic demo content for the teacher, pupil, and school admin portals to feel alive during a pitch walkthrough.

## Changes

- Added a reusable `scripts/seed-demo-content.js` script for the school-live app.
- Seeded the current workspace with three pilot classes: Year 12 Physics A, Year 11 Physics B, and AP Physics.
- Preserved the existing teacher, pupil, and school admin login identities while polishing display names.
- Added 14 anonymous pupil aliases, assignment statuses, questions, stuck signals, check-ins, teacher messages, teacher Agent actions, published materials, draft materials, and audit events.
- Updated `data/workspace-state.json` so teacher analysis, student materials/support, and school admin aggregate comparison have visible demo data immediately.

## Files

- `outputs/teachflow-school-live/scripts/seed-demo-content.js`
- `outputs/teachflow-school-live/data/workspace-state.json`

## Commands Run

- `node scripts/seed-demo-content.js`
- `node --check scripts/seed-demo-content.js`
- `npm.cmd run check`
- `npm.cmd run test:browser-login`

## Decisions

- Kept all pupils alias-only and avoided real names, emails, IDs, or sensitive data.
- Used a reusable seed script so demo content can be restored after future testing or cleanup.

## Issues

- The demo content is intentionally illustrative and deterministic; it is not a real school dataset.

## Next

- Refresh all three portals and use the seeded accounts: `Ms Carter`, `Demo pupil S001`, and `Lead coordinator`.

## Student Material Learning Images

## Goal

Add visual learning materials to the pupil `Materials` page so the demo is not only text-based.

## Changes

- Added three classroom-ready SVG learning visuals:
  - `wave-time-frequency-map.svg`
  - `wave-formula-symbols.svg`
  - `wave-axes-check.svg`
- Updated the demo seed script to publish the visuals as teacher-approved materials.
- Re-seeded `data/workspace-state.json` so `Demo pupil S001` sees two image materials immediately, while other demo pupils also receive targeted visuals.

## Files

- `outputs/teachflow-school-live/assets/wave-time-frequency-map.svg`
- `outputs/teachflow-school-live/assets/wave-formula-symbols.svg`
- `outputs/teachflow-school-live/assets/wave-axes-check.svg`
- `outputs/teachflow-school-live/scripts/seed-demo-content.js`
- `outputs/teachflow-school-live/data/workspace-state.json`

## Commands Run

- `node scripts/seed-demo-content.js`
- `node --check scripts/seed-demo-content.js`
- `npm.cmd run check`
- `npm.cmd run test:browser-login`

## Decisions

- Used local SVG educational diagrams rather than external images so the demo works offline and stays deterministic.
- Kept images as teacher-published materials, preserving the teacher-control model.

## Issues

- These visuals are demo learning aids, not a complete lesson deck.

## Next

- Refresh the student portal and open `Materials` to view the new images.

## Student Material Image Rendering Fix

## Goal

Fix the broken image icons in the pupil `Materials` page.

## Changes

- Added `.svg` MIME support to the local server: `image/svg+xml`.
- Updated the demo seed script to store learning visuals as `data:image/svg+xml` URLs so the currently running old server process does not block rendering.
- Re-seeded `data/workspace-state.json` with the embedded image URLs.

## Files

- `outputs/teachflow-school-live/server.js`
- `outputs/teachflow-school-live/scripts/seed-demo-content.js`
- `outputs/teachflow-school-live/data/workspace-state.json`

## Commands Run

- `node scripts/seed-demo-content.js`
- `node --check scripts/seed-demo-content.js`
- `node --check server.js`
- `npm.cmd run check`
- `npm.cmd run test:browser-login`

## Decisions

- Used embedded SVG data URLs for demo materials so images render immediately after refresh, even before the local server is restarted.

## Issues

- The old running dev server still serves `.svg` files as `application/octet-stream` until restarted, but the embedded material images no longer depend on that path.

## Next

- Refresh the pupil `Materials` page. Restart the local server later so direct `/assets/*.svg` URLs also use the corrected MIME type.

## Public Deployment Config

## Goal

Prepare the current school-live app for public judging access through a cloud URL and class invite QR code.

## Changes

- Updated the root Render deployment config to deploy the active `outputs/teachflow-school-live` app instead of the older `outputs/teachflow-v0.1` demo.
- Renamed the Render service from `teachflow-demo` to `qe-learning-demo` to match the current product direction.

## Files

- `render.yaml`

## Commands Run

- `rg --files -g "render.yaml" -g "package.json" outputs/teachflow-school-live .`
- `Get-Content render.yaml`

## Decisions

- Treat `outputs/teachflow-school-live` as the version to deploy for live judge access because it includes registration, classroom invite links, teacher/student sessions, and the current demo data.

## Issues

- Render free hosting will use temporary server storage unless a database or persistent disk is added, so it is suitable for pitch demos but not final production data retention.

## Next

- Deploy the GitHub repository on Render and generate a QR code for the public classroom join URL.

## Render Public Deployment

## Goal

Deploy the active TeachFlow school-live app to a public URL so judges can join a classroom by scanning a QR code.

## Changes

- Connected Render to the GitHub repository `ljy2652331935-hue/teachflow-ai-school-agent`.
- Created the Render Web Service `qe-learning-demo`.
- Configured Render with:
  - Root Directory: `outputs/teachflow-school-live`
  - Build Command: `npm install`
  - Start Command: `npm start`
  - Instance Type: Free
- Created a public demo classroom on the deployed service.

## Files

- `docs/brain/CURRENT_STATE.md`

## Commands Run

- Used Render dashboard to deploy `qe-learning-demo`.
- Created a public demo class through `POST /api/session/register-teacher`.

## Decisions

- Used Render Web Service instead of a temporary localhost tunnel because the tunnel test returned unstable responses.
- Used a free Render instance for pitch/demo access to avoid adding payment details.

## Issues

- Render free instances can sleep after inactivity, so the first judge request can be slow.
- The deployed repository version still shows older Chinese login copy; the current public deployment is functional, but a final public-demo sync should push the latest local English UI files before the pitch if English UI is required.

## Next

- Public login URL: `https://qe-learning-demo.onrender.com/login.html`
- Judge join URL: `https://qe-learning-demo.onrender.com/login.html?join=join-00c74581cd17b2cef3429fc9`
- Generate a QR code from the judge join URL and open the teacher account `Ms Carter` before the live demo starts.

## Latest GitHub Sync For Public Demo

## Goal

Push the latest local school-live version to GitHub so Render deploys the current English UI and latest teacher/student demo features.

## Changes

- Created a clean deploy worktree from `ljy2652331935-hue/teachflow-ai-school-agent`.
- Mirrored the current project into that deploy worktree while excluding local tools, environment files, logs, Codex metadata, and private local state.
- Verified that no `sk-proj-*` OpenAI key was present in the deploy worktree before commit.

## Files

- `outputs/teachflow-school-live/`
- `docs/brain/`
- `render.yaml`
- `README.md`

## Commands Run

- `git clone https://github.com/ljy2652331935-hue/teachflow-ai-school-agent.git`
- `robocopy ... /MIR` with exclusions for `.env*`, local tools, logs, and private metadata.
- `rg -n 'sk-proj-[A-Za-z0-9_-]+'`
- `npm.cmd run check`

## Decisions

- Push the full current workspace snapshot rather than patching individual files, because the public site was behind the local MVP by many UI, data, agent, and deployment changes.

## Issues

- The current public Render URL may still show the old version until the GitHub push completes and Render finishes redeploying.

## Next

- Commit and push the clean deploy worktree, then verify `https://qe-learning-demo.onrender.com/login.html` shows the latest English UI.

## Public Demo Sync Verified

## Goal

Confirm the latest local version is now the public Render version and regenerate a valid judge classroom QR code.

## Changes

- Pushed commit `d4976f5` to GitHub `main`.
- Verified the Render app serves the current `teachflow-school-live` package and latest public assets.
- Confirmed online `auth-ui.js`, `teacher-prototype.js`, `student-prototype.js`, and `school-admin-prototype.js` have no CJK text.
- Created a fresh live demo class: `Year 12 Physics Demo`.
- Regenerated the local QR image for the current invite link.

## Files

- `outputs/teachflow-school-live/qe-learning-judge-join-qr.png`
- `.gitignore`

## Commands Run

- `git push origin main`
- `Invoke-WebRequest https://qe-learning-demo.onrender.com/login.html`
- `Invoke-WebRequest https://qe-learning-demo.onrender.com/ethereal-ui.css`
- `Invoke-WebRequest https://qe-learning-demo.onrender.com/qe-learning-judge-join-qr.png`
- `Invoke-RestMethod https://qe-learning-demo.onrender.com/api/session/register-teacher`
- `Invoke-RestMethod https://qe-learning-demo.onrender.com/api/session/invite?...`

## Decisions

- Do not push another commit after generating the live invite QR, because a new Render redeploy can reset runtime invite state on the free instance.

## Issues

- The live invite is runtime state on Render's free service. If the service is redeployed or restarted, generate a new invite and QR before the pitch.

## Next

- Use the public login URL `https://qe-learning-demo.onrender.com/login.html`.
- Use the judge join URL `https://qe-learning-demo.onrender.com/login.html?join=join-6d37d5ced42095b94db1775a`.

## Stable Public Invite Fix

## Goal

Fix the public QR flow after scanning showed `This invite link is not valid`.

## Changes

- Added a stable public demo invite token, `join-demo`.
- Updated the backend to backfill `join-demo` when persisted Render state already has a class but no valid invite link.
- Updated the backend so a `join-demo` scan creates a `Year 12 Physics Demo` class when Render has reset to an empty workspace.
- Added a regression test proving `join-demo` resolves to the existing class and can register a pupil.
- Updated the school-live check script to run the new public invite test.

## Files

- `outputs/teachflow-school-live/workspace-store.js`
- `outputs/teachflow-school-live/package.json`
- `outputs/teachflow-school-live/tests/public-demo-invite.test.js`
- `docs/brain/CURRENT_STATE.md`
- `docs/brain/devlog/2026-07-04.md`

## Commands Run

- `npm.cmd run check`
- `Invoke-RestMethod https://qe-learning-demo.onrender.com/api/session/invite?token=join-6d37d5ced42095b94db1775a`
- `Invoke-RestMethod https://qe-learning-demo.onrender.com/api/session/accounts`

## Decisions

- Use a stable invite token instead of repeatedly creating one-off runtime invite links, because Render free service restarts can invalidate runtime-only links.

## Issues

- The current online service must redeploy before `join-demo` becomes valid.

## Next

- Push the fix, wait for Render redeploy, verify `https://qe-learning-demo.onrender.com/login.html?join=join-demo`, and regenerate the QR image.
